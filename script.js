// create a const to select the body in HTML

// use this const to select the element

// toggle this class by selecting the function

function dark_mode() {
  const darkMode = document.body;
  darkMode.classList.toggle("dark-mode");
}
